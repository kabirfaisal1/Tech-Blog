
<section class="vh-100">
    <br>
    <div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
     
    <div id="comment-text" class="text">
      {{comment_text}}
    </div>

  </section>
  {{/each}}
</div>
    <br>
</section >

